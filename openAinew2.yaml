openapi: 3.0.3
info:
  title: APIs REST
  description: |-
    Lista de APIs REST disponibilizadas pelo TOTVS appServer.
    
    Porta: [9995]
  version: 0.1.1
paths:
  /api/dashboard/userProfiles:
    post:
      summary: userProfiles
      description: Exemplo para geração de documentação Swagger, metodo- Post
      parameters: []
      responses: 
        200:
          description: Success
        400:
          description: Bad Request
    put:
      summary: userProfiles
      description: Exemplo para geração de documentação Swagger, metodo- put
      parameters:
        - name: id
          in: path
          description: desc_id_1
          required: true
          schema: 
            type: string
      responses: 
        200:
          description: Success
        400:
          description: Bad Request
    get:
      summary: userProfiles
      description: Exemplo para geração de documentação Swagger, metodo- Get.
      parameters:
        - name: id
          in: path
          description: desc_id_1
          required: true
          schema: 
            type: string
      responses: 
        200:
          description: Success
        400:
          description: Bad Request